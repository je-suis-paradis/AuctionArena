info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5147/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "pnut@test.com",
        "password": "Test123!"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
