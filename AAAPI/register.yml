info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5147/api/auth/register
  body:
    type: json
    data: |-
      {
        "username": "pnut",
        "email": "pnut@test.com",
        "password": "Test123!"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
